# Copy to .env (git-ignored) for local runs, or enter interactively in the
# Colab notebooks via getpass -- never commit a real key.
OPENAI_API_KEY=

# Optional overrides (see src/foundry/config.py for defaults)
FOUNDRY_DB_PATH=
FOUNDRY_LEASE_SECONDS=
